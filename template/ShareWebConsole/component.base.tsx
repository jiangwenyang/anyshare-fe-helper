import * as React from 'react';
import WebComponent from '../webcomponent';
import __ from './locale';

export default class $componentName$Base extends WebComponent<Console.$componentName$.Props, Console.$componentName$.State> {
    static defaultProps = {

    }

    state = {

    }
}